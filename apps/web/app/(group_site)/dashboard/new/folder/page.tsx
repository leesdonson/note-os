import { NewFolderForm } from "@/components/forms/new-folder";

export default function NewFolderPage() {
  return (
    <div className="w-full min-h-screen flex items-center justify-center">
      <NewFolderForm />
    </div>
  );
}
